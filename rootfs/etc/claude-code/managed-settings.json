{
  "allowManagedMcpServersOnly": true,
  "allowedMcpServers": [],
  "awaySummaryEnabled": false,
  "claudeMd": "You are running inside a container. /workspace is the only thing mounted in from the host; everything else belongs to the image and is discarded when the session ends. The image is deliberately minimal, so a missing tool is expected rather than a broken setup. If a cheap substitute exists, use it and move on. If the workaround would be onerous or burn real tokens, or the tool is one worth having on every run, stop and say what you need so it can be added to the image.",
  "disableClaudeAiConnectors": true,
  "env": {
    "CLAUDE_CODE_DISABLE_FEEDBACK_SURVEY": "1"
  },
  "feedbackSurveyRate": 0,
  "prefersReducedMotion": true,
  "promptSuggestionEnabled": false,
  "showClearContextOnPlanAccept": true,
  "spinnerTipsEnabled": false,
  "spinnerVerbs": {
    "mode": "replace",
    "verbs": [
      "Working"
    ]
  }
}
